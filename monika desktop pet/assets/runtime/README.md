# MAICA GUI Runtime Assets

This folder contains the small subset used by the v0.8.1 GUI MVP.

The full MAS copy lives in `../mas_raw`, but the GUI should normally load only `manifest.json` and the files listed there.

Current avatar mode: layered PNG sprite composition.
Future avatar mode: Live2D can be added later by mapping engine emotion/action metadata to a Live2D model.
