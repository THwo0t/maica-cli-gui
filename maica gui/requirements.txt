# MAICA GUI runtime dependencies.

# Required: Qt bindings for the desktop GUI.
PySide6>=6.8

# Optional: Aliyun Bailian CosyVoice TTS + Paraformer STT providers
# (cross-platform, need a DashScope API key).
websocket-client

# Optional: microphone capture for Bailian Paraformer STT (stt_provider = bailian_paraformer).
# Linux also needs the PortAudio system library (e.g. libportaudio2 / portaudio).
sounddevice
numpy

# --- Cross-platform audio / voice notes (not pip-installable) ---
#
# TTS playback uses QtMultimedia (QMediaPlayer/QAudioOutput). Linux needs a
# working PipeWire or PulseAudio backend. Legacy direct provider calls can
# still use ffplay/mpv/afplay/PowerShell playback where available.
#
# Local offline TTS (tts_provider = system_say / auto):
#   macOS  : `say` ships with the OS
#   Linux  : install `speech-dispatcher` (spd-say) or `espeak-ng`
#   Windows: System.Speech via PowerShell (tts_provider = windows_sapi)
#
# STT (speech-to-text) currently has a Windows-only local engine.
# On macOS/Linux, stt_provider = auto resolves to off.
